<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <style>
      /* Center the content horizontally */
      .center-content {
        text-align: center;
        margin-top: 20px;
      }

      /* Circular profile picture */
      .profile-picture {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        overflow: hidden;
      }
    </style>
    <title>Profile Page</title>
  </head>
  <body>
    <div class="container mt-3">
      <div class="center-content">
        <!-- Circular Profile Picture -->
        <div class="profile-picture">
          <img
            src="/images/admin/manager.png"
            alt="image-not-found"
            class="img-fluid"
          />
        </div>

        <!-- Name -->
        <h2 class="mt-3">Admin</h2>
      </div>
    </div>

    <section class="container">
      <a href="/ceo/add-dept" class="btn btn-success btn-block my-3">Verified preneurs</a>
      <table class="table table-bordered table-responsive-sm">
        <thead>
          <tr>
            <th scope="col">Username</th>
            <th scope="col">Date Applied</th>
            <th scope="col">Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#each users}}
          <tr>
           <td>{{this.username}}</td>
            <td>{{this.applieddate}}</td>
            <td>
              <a href="/admin/review-application?id={{this._id}}&username={{this.username}}" class="btn btn-primary btn-sm mr-2">
                Review Application
              </a>
              <a href="/ceo/show-details?id={{this._id}}" class="btn btn-secondary btn-sm">
                View More Details
              </a>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </section>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"
    ></script>
    <script
      src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"
    ></script>
  </body>
</html>
